apiVersion: apps/v1
kind: Deployment
metadata:
  name: retail-frontend
  namespace: production
  labels:
    app: retail-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: retail-frontend
  template:
    metadata:
      labels:
        app: retail-frontend
    spec:
      containers:
      - name: web-ui
        image: umasankar33/frentend:1
        ports:
        - containerPort: 80
          name: http
        env:
        # 🔄 CHANGE THIS: Update to the environment variable your frontend code uses to look for the API
        - name: REACT_APP_API_BASE_URL 
          value: "http://localhost:5000" # 🔌 Forces the UI to fetch data straight from your active local backend proxy window!
        resources:
          limits:
            cpu: "200m"
            memory: "128Mi"
          requests:
            cpu: "100m"
            memory: "64Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: retail-frontend-service
  namespace: production
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: retail-frontend